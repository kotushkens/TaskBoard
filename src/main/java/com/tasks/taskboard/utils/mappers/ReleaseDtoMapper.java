package com.tasks.taskboard.utils.mappers;

/**
 * Класс для конвертации объектов в DTO и наоборот
 */

public class ReleaseDtoMapper {
 /*   public ReleaseEntity mapDtoToEntity(ReleaseRequestDto request){
        ReleaseEntity entity = new ReleaseEntity();
        entity.setId(request.getId());
        entity.setVersion(request.getVersion());
        entity.setStart(request.getStart());
        entity.setEnd(request.getEnd());
    }*/
}
